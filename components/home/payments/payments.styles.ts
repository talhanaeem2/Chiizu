const h1 = `lg:text-[50px]/10x2 md:text-4xl/10 sm:text-3xl/9 text-2xl/8 sm:font-bold font-semibold
px-[30px] text-left sm:text-center`;

const paymentCardRoot = "sm:px-24 sm:pt-12 sm:mb-16 px-7 pt-5 mb-10 flex justify-center items-center";

const paymentCardContainer = "grid grid-cols-1 gap-12 md:grid-cols-[50%_minmax(558px,_2fr)]";

const bgWhite = `bg-whiteBlue text-center text-darkJungleGreen pt-[40px] px-[46px] max-[340px]:px-[25px]
pb-[40px] md:pt-[74px] md:pb-[62px] lg:px-44 md:px-20`;

const bgWhiteH1 = "lg:text-[50px]/10x2 md:text-4xl/10 sm:text-3xl/9 text-2xl/8 sm:font-bold font-semibold";

const bgWhiteP = "py-7 sm:text-xl/10 text-sm/6 px-9";

const sponsors = "flex justify-between items-center py-7 px-7 flex-wrap md:flex-row";

const componentsContainer = "pt-[4px] md:pt-[20px] flex justify-center flex-col sm:flex-row px-7";

const input = `w-full rounded-full py-3 px-5 sm:pl-5 sm:py-3 border-0 sm:pr-40 text-darkJungleGreen
placeholder:text-darkJungleGreen focus:ring-2 focus:ring-inset focus:ring-clearBlue sm:text-sm sm:leading-6`;

const chatButton = `mt-3 sm:mt-0 self-center sm:text-lg/6 inline-flex items-center rounded-full
bg-transparent px-4 sm:px-7 py-1 sm:py-3 text-xs font-semibold
text-darkJungleGreen hover:text-persianGreen focus-visible:outline focus-visible:outline-2`;

const root = "text-center mt-12 pb-10";

const gradientIndigo = "bg-gradient-to-b from-lavenderIndigo to-white";

const gradientYellow = "bg-gradient-to-b from-naplesYellow to-white";

const gradientGreen = "bg-gradient-to-b from-greenishCyan to-white";

const gradientBlue = "bg-gradient-to-b from-clearBlueGradient to-white";

const buttonContainer = "flex flex-col";

const textClearBlue = "text-clearBlue";

const smMarginRight5 = "sm:mr-5";

const paymentStyles = {
    h1,
    paymentCardRoot,
    paymentCardContainer,
    bgWhite,
    bgWhiteH1,
    bgWhiteP,
    sponsors,
    componentsContainer,
    input,
    chatButton,
    root,
    gradientIndigo,
    gradientYellow,
    gradientGreen,
    gradientBlue,
    buttonContainer,
    textClearBlue,
    smMarginRight5
};

export default paymentStyles;