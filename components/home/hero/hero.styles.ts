const root = "bg-darkJungleGreen text-center text-white pt-16 sm:pt-40";

const h1 = "lg:text-[70px]/10x2 md:text-5xl sm:text-4xl/8 text-2xl/9 pb-10 sm:font-bold font-semibold";

const p = "pb-4 lg:text-xl/10 md:text-lg/8 sm:text-base/7 text-sm/6 px-9";

const perksSpan = "pl-3 sm:text-base text-xs leading-4";

const sponsors = `[&>*:nth-last-child(-n+3)]:hidden md:[&>*:nth-last-child(-n+3)]:flex
flex justify-between lg:pt-7 lg:px-7 pt-6 min-[340px]:px-4 max-[320px]:px-[17px] mt-28 bg-white flex-wrap`;

const componentsContainer = "pt-10 flex justify-center flex-col sm:flex-row px-7";

const buttonContainer = "flex flex-col gap-[14px]";

const textClearBlue = "text-clearBlue";

const paddingRight5 = "pr-5";

const inlineBlock = "inline-block";

const smMarginRight5 = "sm:mr-5";

const perksRoot = "flex justify-center";

const sponsorImage = "mr-[24px] md:mr-0 max-[650px]:mb-[24px] sm:mb-0";

const heroStyles = {
    root,
    h1,
    p,
    perksSpan,
    sponsors,
    componentsContainer,
    buttonContainer,
    textClearBlue,
    paddingRight5,
    inlineBlock,
    smMarginRight5,
    perksRoot,
    sponsorImage
};

export default heroStyles;