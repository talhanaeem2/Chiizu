const rootH1 = "sm:text-5xl text-2xl sm:leading-10x2 leading-9 font-bold";

const carouselItem = `rounded-3xl bg-white shadow-md w-80 sm:h-[345px] sm:mt-10 sm:mb-16 sm:mr-5
 my-5 flex flex-col p-7`;

const carouselItemH3 = "pb-1 sm:text-lg text-sm font-semibold text-riverBed";

const carouselItemP = "sm:text-sm text-xs font-normal text-riverBed";

const root = "sm:mt-16 mt-5 sm:pl-24 p-5";

const rootP = "sm:text-xl";

const carouselContainer = "flex flex-col sm:flex-row";

const itemContainer = "flex pb-5";

const carouselDesc = "sm:text-lg text-sm font-normal";

const textClearBlue = "text-clearBlue";

const paddingLeft5 = "pl-5";

const storiesStyles = {
    rootH1,
    carouselItem,
    carouselItemH3,
    carouselItemP,
    root,
    rootP,
    carouselContainer,
    itemContainer,
    textClearBlue,
    paddingLeft5,
    carouselDesc
};

export default storiesStyles;