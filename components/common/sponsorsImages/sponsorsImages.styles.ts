const sponsors = "[&>*:nth-last-child(-n+3)]:hidden md:[&>*:nth-last-child(-n+3)]:flex";

const bgWhite = "bg-white";

const commonSponsors = "flex justify-between py-7 mt-28 px-7 flex-wrap";

const imageSpacing = "mb-[20px] md:mb-0";

const sponsorsImagesStyles = {
    sponsors,
    bgWhite,
    commonSponsors,
    imageSpacing
};

export default sponsorsImagesStyles;