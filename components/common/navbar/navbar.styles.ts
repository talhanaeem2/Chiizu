const root = "flex bg-darkJungleGreen h-full justify-between py-6 lg:px-24 px-7";

const w40 = "w-[40%]";

const buttonContainer = "w-[60%] justify-end flex";

const navbarStyles = {
    root,
    w40,
    buttonContainer
};

export default navbarStyles;