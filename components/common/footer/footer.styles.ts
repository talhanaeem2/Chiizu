const footer = `bg-darkJungleGreen flex md:justify-center md:items-center justify-start
text-white flex-col pt-10 md:pt-24 pb-7 px-[30px]`;

const social = "flex flex-wrap md:flex-row md:justify-center md:items-center";

const socialInner = "flex items-center md:pr-5 pb-5 pr-3 hover:text-persianGreen";

const footerBottom = `md:pt-8 flex flex-col md:flex-row text-center md:px-24
md:bg-darkJungleGreen text-white md:w-full md:justify-center md:items-center`;

const footerTextContainer = "pb-[30px] md:w-full text-left";

const footerText = "text-[11px] leading-5 md:text-center md:text-[10px] tracking-[0.44px] md:px-[24%]";

const privacyPolicytext = "text-xs mr-5";

const paddingBottom12 = "pb-12";

const paddingTop5 = "pt-5";

const paddingLeft3 = "pl-[11px]";

const textXs = "text-xs";

const rootCopyright = "bg-white flex flex-col items-center md:flex-row md:justify-around my-6 px-[50px] sm:px-[100px]";

const copyrightContainer = "w-full text-center md:text-left";

const poweredContainer = "my-[15px] w-full flex justify-center";

const poweredText = "mr-[7px] text-[11px]";

const privacyContainer = "w-full md:text-right text-center";

const footerStyles = {
    footer,
    social,
    socialInner,
    footerBottom,
    footerTextContainer,
    footerText,
    privacyPolicytext,
    paddingBottom12,
    paddingTop5,
    paddingLeft3,
    textXs,
    rootCopyright,
    copyrightContainer,
    poweredContainer,
    poweredText,
    privacyContainer
};

export default footerStyles;